# Opcional en desarrollo: API en otro puerto
# VITE_API_URL=http://127.0.0.1:8000
#
# Si no definís VITE_API_URL, `npm run dev` usa el proxy de vite.config.ts hacia :8000.
#
# Producción (Railway): un solo Dockerfile en la raíz del repo — no hace falta VITE_API_URL.
